@{
    ViewBag.Title = "Dashboard";
    ViewBag.PageTitle = "Customer Dashboard";
    ViewBag.Breadcrumb = "Home / Dashboard";
}

<style>
    /* Responsive Stats Grid */
    .stats-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 24px;
        margin-bottom: 32px;
    }

    /* Responsive Quick Actions */
    .quick-actions-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 16px;
        padding: 20px;
    }

    .action-card {
        text-align: center;
        padding: 24px 20px;
        text-decoration: none;
        cursor: pointer;
        background: white;
        border-radius: 16px;
        border: 2px solid #e2e8f0;
        transition: all 0.3s ease;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 12px;
    }

    .action-card:hover {
        transform: translateY(-4px);
        box-shadow: 0 12px 24px rgba(99, 102, 241, 0.15);
        border-color: var(--primary);
    }

    .action-icon {
        width: 64px;
        height: 64px;
        border-radius: 16px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 32px;
        margin: 0 auto;
    }

    .action-icon.green {
        background: linear-gradient(135deg, #10b981, #059669);
    }

    .action-icon.yellow {
        background: linear-gradient(135deg, #f59e0b, #d97706);
    }

    .action-icon.purple {
        background: linear-gradient(135deg, #8b5cf6, #7c3aed);
    }

    .action-icon.blue {
        background: linear-gradient(135deg, #3b82f6, #2563eb);
    }

    .action-label {
        font-weight: 600;
        color: var(--text-primary);
        font-size: 15px;
    }

    /* Mobile - Stack vertically */
    @@media (max-width: 767px) {
        .stats-grid {
            grid-template-columns: 1fr !important;
            gap: 16px;
        }

        .quick-actions-grid {
            grid-template-columns: repeat(2, 1fr) !important;
            gap: 12px;
            padding: 16px;
        }

        .action-card {
            padding: 20px 16px;
        }

        .action-icon {
            width: 56px;
            height: 56px;
            font-size: 28px;
        }

        .stat-value {
            font-size: 24px !important;
        }
    }

    /* Tablet - 2 columns for stats, 2x2 for actions */
    @@media (min-width: 768px) and (max-width: 1023px) {
        .stats-grid {
            grid-template-columns: repeat(2, 1fr) !important;
            gap: 20px;
        }

        .quick-actions-grid {
            grid-template-columns: repeat(2, 1fr) !important;
            gap: 16px;
        }
    }

    /* Laptop - 3 columns */
    @@media (min-width: 1024px) and (max-width: 1439px) {
        .stats-grid {
            grid-template-columns: repeat(3, 1fr) !important;
        }

        .quick-actions-grid {
            grid-template-columns: repeat(4, 1fr) !important;
        }
    }
</style>

<div class="stats-grid">
    <div class="stat-card" style="border-left: 4px solid #6366f1;">
        <div class="stat-icon" style="background: linear-gradient(135deg, #6366f1, #4f46e5); color: white;">🏦</div>
        <div class="stat-label">Account Number</div>
        <div class="stat-value" style="font-size: 18px;">@ViewBag.AccountNumber</div>
        <div class="stat-change">@ViewBag.AccountType Account</div>
    </div>
    <div class="stat-card" style="border-left: 4px solid #10b981;">
        <div class="stat-icon" style="background: linear-gradient(135deg, #10b981, #059669); color: white;">💰</div>
        <div class="stat-label">Current Balance</div>
        <div class="stat-value" style="color: #10b981;">$@ViewBag.Balance?.ToString("N2")</div>
        <div class="stat-change">Available Balance</div>
    </div>
    <div class="stat-card" style="border-left: 4px solid #8b5cf6;">
        <div class="stat-icon" style="background: linear-gradient(135deg, #8b5cf6, #7c3aed); color: white;">💸</div>
        <div class="stat-label">Recent Transactions</div>
        <div class="stat-value" style="color: #8b5cf6;">@(ViewBag.RecentTransactions?.Count ?? 0)</div>
        <div class="stat-change">Last 10 Transactions</div>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <div class="card-title">⚡ Quick Actions</div>
    </div>
    <div class="quick-actions-grid">
        <a href="@Url.Action("Deposit", "Customer")" class="action-card">
            <div class="action-icon green">💵</div>
            <div class="action-label">Deposit</div>
        </a>
        <a href="@Url.Action("Withdraw", "Customer")" class="action-card">
            <div class="action-icon yellow">💸</div>
            <div class="action-label">Withdraw</div>
        </a>
        <a href="@Url.Action("Transfer", "Customer")" class="action-card">
            <div class="action-icon purple">🔄</div>
            <div class="action-label">Transfer</div>
        </a>
        <a href="@Url.Action("PayBills", "Customer")" class="action-card">
            <div class="action-icon blue">📄</div>
            <div class="action-label">Pay Bills</div>
        </a>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <div class="card-title">Recent Transactions</div>
        <a href="@Url.Action("Transactions", "Customer")" class="view-all-btn">View All</a>
    </div>
    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>Transaction ID</th>
                    <th>Type</th>
                    <th>Amount</th>
                    <th>Date</th>
                    <th>Status</th>
                    <th>Reference</th>
                </tr>
            </thead>
            <tbody>
                @if (ViewBag.RecentTransactions != null && ViewBag.RecentTransactions.Count > 0)
                {
                    @foreach (var transaction in ViewBag.RecentTransactions)
                    {
                        <tr>
                            <td>#TXN@transaction.TransactionId</td>
                            <td>@transaction.TransactionType</td>
                            <td><strong>$@transaction.Amount.ToString("N2")</strong></td>
                            <td>@transaction.TransactionDate.ToString("yyyy-MM-dd hh:mm tt")</td>
                            <td><span class="status-badge status-@transaction.Status.ToLower()">@transaction.Status</span></td>
                            <td>@transaction.ReferenceNumber</td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="6" style="text-align: center;">No transactions yet</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
